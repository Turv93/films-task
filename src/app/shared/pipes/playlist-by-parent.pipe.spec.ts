import { PlaylistByParentPipe } from './playlist-by-parent.pipe';

describe('PlaylistByParentPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaylistByParentPipe();
    expect(pipe).toBeTruthy();
  });
});
